import React from "react";
import SearchInput from "../SearchInput";
import MovieList from "../MovieList";

const Home = () => {
  return (
    <div>
        hello, world
        <SearchInput />
        <MovieList />
    </div>
  );
};

export default Home;
